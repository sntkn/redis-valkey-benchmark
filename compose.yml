services:
  valkey:
    image: valkey/valkey:latest
    ports:
      - 6379:6379
    volumes:
      - valkey:/data
  redis:
    image: redis:latest
    ports:
      - 6380:6380
    volumes:
      - redis:/data
volumes:
  valkey:
  redis:
